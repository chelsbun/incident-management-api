# Purpose: Example environment variables for local development (no secrets).
# Author: (project)
# Last Modified: 2026-02-12
#
# Copy this file to `.env` and replace placeholder values.

# SQLAlchemy connection string used by the API and migrations.
# Example:
# postgresql+psycopg2://incident_user:CHANGE_ME@localhost:5432/incident_db
DATABASE_URL=postgresql+psycopg2://incident_user:CHANGE_ME@localhost:5432/incident_db

# Optional: values used by docker-compose (if you wire it up to read from .env)
POSTGRES_USER=incident_user
POSTGRES_PASSWORD=CHANGE_ME
POSTGRES_DB=incident_db

